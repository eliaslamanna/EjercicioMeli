package model;

public class SatelliteSato extends Satellite{

    private Coordinate coordinates = new Coordinate(500,100);

    public SatelliteSato(float distance) {
        this.distance = distance;
    }
}
